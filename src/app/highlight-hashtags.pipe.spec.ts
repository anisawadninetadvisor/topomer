import { HighlightHashtagsPipe } from './highlight-hashtags.pipe';

describe('HighlightHashtagsPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightHashtagsPipe();
    expect(pipe).toBeTruthy();
  });
});
